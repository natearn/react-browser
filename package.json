{
  "name": "react-browser",
  "version": "0.0.1",
  "description": "A file browser for anything, but most likely for CMS entities",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "babelify": "^7.3.0",
    "browserify": "^13.1.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify --debug -t [ babelify --presets [ es2015 react ] ] application.es6 > application.js"
  },
  "keywords": [
    "react"
  ],
  "author": "Nathan Arnold (natearn@natearn.info)",
  "license": "ISC"
}
